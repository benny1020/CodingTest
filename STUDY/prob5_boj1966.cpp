#include <iostream>

using namespace std;

int main(void)
{
    int num, size, tmp;
    int cur=0;
    cin >> num;

    while(num--)
    {
        cin >> size;
        cin >> tmp;
        int arr[size];
        for(int i=0;i<size;i++)
            cin >> arr[size];

    }
}
